<div class="accroche">
  <h2>Vos réservations</h2>
</div>

  <%= link_to "← Retour", root_path, class:"text-decoration-none text-dark ms-5 "%>

<div class="container">

  <% if !@bookings.exists?() %>
    <div class="no-booking">
      <h5>Vous n'avez pas encore de réservation 🥲</h5>
      <h6> Pour consultez nos offres, cliquer <%= link_to "ici", root_path%></h6>
    </div>
  <%else %>

    <div class="grid-card">
        <% @bookings.each do |booking| %>
          <div class="card-product">
            <div class="photo-book">
              <img src="<%=booking.room.image_url%>" style ="width: 300px; heigth: 100%">
            </div>
            <div class="card-product-infos">
              <h2><strong><%= booking.room.category %></strong></h2>
              <p>Du <%=booking.start_date %> au <%=booking.end_date %></p>
              <p><%= booking.room.price %> €/nuit</p>
              <%# <div class="icon-delete"> %>
                <div class="d-flex">
                  <%= link_to 'Modifier',  edit_booking_path(booking), class: "btn btn-primary button" %>
                  <%# <div class="icon-delete"> %>
                    <%= link_to "🗑️", destroy_booking_path(booking), data: {turbo_method: :delete, turbo_confirm: "Etes-vous sur.e?"}, class: "supprimer"%>
                  <%# </div> %>
                </div>
              <%# </div> %>
            </div>
          </div>
        <% end %>
    </div>
  <% end %>
</div>
